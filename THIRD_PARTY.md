# Third-party notices

This repository currently contains a small private compatibility shim under `src/ohmni/_vendor/agent_harness/`.

The full upstream harness package has not been copied into this checkout yet. When the upstream code is vendored, record:

- exact upstream commit;
- upstream license text;
- local patches;
- update date.
